# Python 练习速查表

> 蓝酱整理 · 覆盖 practice01 ~ practice14 + 练习/ 目录
> 每个知识点一句话，配合代码文件复习用

## 基础与类型转换
- `print()` 输出；字符串用单引号或双引号包裹
- 类型转换：`int()`（截断取整，不四舍五入）、`float()`、`str()`、`bool()`
- `bool` 为假的值：`0`、空串 `''`、`None`、空容器
- `chr(编码) → 字符`，`ord(字符) → 编码`，互为反操作
- `input()` 读到的永远是字符串，算数前要先转 `int()` / `float()`

## 运算符
- 算术：`+ - * / // % **`（`//` 整除、`%` 取余、`**` 幂）
- 赋值简写：`+= -= *= /= //= %=`
- 海象 `:=`：赋值的同时返回这个值
- 比较：`== != > < >= <=`，结果是布尔
- 逻辑：`and` 全真才真、`or` 有真即真、`not` 取反；优先级 `not > and > or`
- 成员：`in` / `not in`
- 身份：`is` 比对象（内存地址）、`==` 比值；小整数池 `-5 ~ 256` 复用同一个对象

## 流程控制
- `if / elif / else`：多分支；`elif` 相当于 `else + if`
- `match / case`（Python 3.10+）：按值匹配，`case _` 兜底，`|` 连接多值，`case x` 绑定变量
- `for` + `range(起, 止, 步长)`：左闭右开；`range(n)` 是 0 ~ n-1
- `while`：条件为真一直跑；`break` 立刻退出，`continue` 跳过本次
- 三元式：`真值 if 条件 else 假值`

## 字符串
- 转义 `\n` 换行；原始字符串 `r'...'` 不转义；三引号定义多行
- 切片 `[start:end:step]` 取头不取尾，`step` 为负反向取
- `len()` 长度；`+` 拼接；`*` 重复
- 格式化：`%s` 字符串、`%d` 整数、`%f` 浮点、`%.2f` 保留两位；`f'{变量:.2f}'`
- `f'{变量=}'` 打印「变量名=值」，调试神器
- 方法：`upper/lower/title/capitalize`、`find/index`（find 找不到返回 -1，index 报错）、`startswith/endswith`、`isdigit/isalpha/isalnum`、`split/join`、`replace`、`center/rjust/ljust`
- 编码：`encode('utf-8')` 字符串转字节，`decode` 转回，编解码字符集要一致

## 列表 list
- `[]` 定义，元素可重复、可不同类型；`list()` 转换
- 索引从 0 开始，`-1` 是最后一个；越界抛 `IndexError`
- 切片 `[start:end:stride]` 左闭右开
- 方法：`append` 末尾加、`insert(下标, 值)`、`remove(值)`、`pop(下标)` 删并返回、`clear`、`del`、`count`、`index`、`sort`、`reverse`
- `pop()` 不写下标 = 删最后一个并返回；`sort/reverse` 原地修改
- 生成式：`[表达式 for 变量 in 可迭代 if 条件]`
- 二维列表：列表套列表，`a[i][j]` 两层下标

## 元组 tuple
- `(x, y, z)`，不可变：不能增删改
- 单元素元组要加逗号 `(1,)`；`(1)` 只是括号不是元组
- 打包 / 解包：`a, b, c = t`；`*变量` 收集多余元素成列表
- 交换变量：`a, b = b, a`（不用临时变量）

## 集合 set
- `{}` 或 `set()`，无序、不重复
- 运算：`&` 交集、`|` 并集、`-` 差集、`^` 对称差
- `in` 比列表快（哈希查找）
- 方法：`add`、`remove`（不存在报错）、`clear`、`isdisjoint`
- `frozenset` 不可变集合
- 带 `update` 后缀的方法：原地修改、返回 `None`

## 字典 dict
- `{}` 或 `dict()`，键唯一且不可变
- 取值 `d[key]`（键不存在抛 `KeyError`）；`d.get(key, 默认值)` 安全
- 遍历 `for k in d` 拿键、`d[k]` 拿值、`d.items()` 拿键值对
- 方法：`keys/values/items`、`update`（合并，同键覆盖）、`pop`（删并返回）、`popitem`（删最后一对）、`del`
- 成员 `in` 判断的是键，不是值

## 函数 def
- `def 名(参数): ... return 值`；不写 `return` 默认返回 `None`
- 位置参数按顺序对应；关键字参数 `名=值` 可乱序
- `/` 左边只能位置传参；`*` 右边只能关键字传参
- 默认值：`def f(n=5)`；无默认值的参数必须写在有默认值的前面
- `*args` 收位置参数成元组；`**kwargs` 收关键字参数成字典
- 循环变量别和参数同名（参数会被覆盖）

## 模块与导入
- `import math` → 用 `math.pi`；`from math import factorial as fac` 起别名
- 自己的 `.py` 文件就是模块，可被 `import`
- `from 模块 import 函数` 直接用函数名；`import 模块` 用 `模块.函数`

## 常用内置函数
- `len` 长度、`sum` 求和、`min/max` 最值、`type` 类型、`id` 内存地址
- `random.randint(a, b)` 含两端；`random.randrange(a, b)` 含头不含尾
- `math.sqrt` 开根号、`math.factorial` 阶乘、`math.pi` 圆周率

## 易错点（重点背）
- `input()` 是字符串，加减前要转换
- `int()` 截断，不是四舍五入
- 列表 / 字典 / 集合的方法大多**原地修改、返回 `None`**
- 索引越界 `IndexError`；字典缺键 `KeyError`
- `is` 和 `==` 不是一回事
- `range` 左闭右开，容易漏掉右端点
